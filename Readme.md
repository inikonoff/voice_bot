# 🤖 iГрамотей v4

**iГрамотей** — Telegram-бот для интеллектуальной обработки текста. Транскрибирует голосовые сообщения и кружочки, распознаёт текст с изображений (OCR), читает документы (PDF, DOCX, TXT) и предлагает три режима обработки: коррекцию, редактуру или саммари.

---

## 🚀 Возможности

### 🎙️ Транскрибация
- **Голосовые сообщения** и **кружочки** — автоматическое распознавание речи через Whisper (Groq)
- Ротация пула API-ключей с обработкой Rate Limit

### 📄 Работа с файлами и изображениями
- **OCR:** распознавание текста с фото и скриншотов через Groq Vision
- **PDF:** извлечение текста и таблиц через `pdfplumber`
- **DOCX:** чтение документов Word через `python-docx`
- **TXT:** поддержка UTF-8, CP1251 и других кодировок

### ✍️ Режимы обработки текста
- 📝 **Как есть** — минимальная коррекция: опечатки, пунктуация, регистр
- ✨ **Красиво** — редактура с сохранением голоса автора
- 📊 **Саммари** — аналитический пересказ от третьего лица (для длинных текстов)
- ✏️ **Работа над ошибками** — разбор правок с объяснениями
- 💬 **Диалог по документу** — вопросы к содержимому загруженного текста (в режиме саммари)

### 💾 Экспорт
- Выгрузка результата в **TXT**, **PDF** (reportlab) или **DOCX**

---

## 🛠️ Технологический стек

| Слой | Технология |
|---|---|
| Bot Framework | [aiogram 3.x](https://docs.aiogram.dev/) |
| Web Server | [FastAPI](https://fastapi.tiangolo.com/) + [uvicorn](https://www.uvicorn.org/) |
| LLM / STT API | [Groq Cloud](https://groq.com/) (Llama 4, Llama 3.3, Whisper) |
| OCR | Groq Vision (llama-4-scout) |
| PDF | pdfplumber (чтение), reportlab (запись) |
| DOCX | python-docx |
| База данных | [Supabase](https://supabase.com/) (опционально, с полным fallback) |
| Мультимедиа | ffmpeg (для кружочков) |
| Деплой | [Render.com](https://render.com/) |

---

## 📦 Установка и запуск

### 1. Системные зависимости
```bash
sudo apt-get update && sudo apt-get install -y ffmpeg
```

### 2. Python-пакеты
```bash
pip install -r requirements.txt
```

### 3. Переменные окружения (`.env`)
```env
BOT_TOKEN=ваш_токен_телеграм_бота
GROQ_API_KEYS=ключ1,ключ2,ключ3
SUPABASE_URL=https://xxx.supabase.co   # опционально
SUPABASE_KEY=ваш_anon_ключ             # опционально
PORT=8080
```

> Без `SUPABASE_URL` / `SUPABASE_KEY` бот работает без базы данных — история `/history` недоступна.

### 4. Запуск
```bash
python bot.py
```

---

## 🌐 Деплой на Render.com

1. Создайте **Web Service**, укажите команду запуска `python bot.py`
2. Добавьте переменные окружения из `.env`
3. В `Build Command` добавьте установку ffmpeg:
   ```bash
   apt-get install -y ffmpeg && pip install -r requirements.txt
   ```
4. Бот автоматически поднимает FastAPI на порту `$PORT` — Render определит его как открытый порт
5. Health check доступен по `/health`

---

## 🏗️ Структура проекта

```
bot.py          — хендлеры, клавиатуры, роутинг
processors.py   — OCR, транскрибация, коррекция, экспорт
config.py       — промпты, константы, тексты сообщений
database.py     — Supabase-слой с полным fallback
```

---

## 📋 Команды бота

| Команда | Описание |
|---|---|
| `/start` | Описание бота |
| `/help` | Инструкция по использованию |
| `/history` | Последние 10 обработок (требует Supabase) |
| `/status` | Техническое состояние бота |

---

## 📝 Лицензия
Проект для частного использования. Все права на используемые API принадлежат их владельцам.
