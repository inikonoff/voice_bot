# CHANGELOG v3.0 — Поддержка видео и видеоплатформ

## 🎉 Основные улучшения

### 1️⃣ **Обработка локальных видеофайлов** ✅
- Поддержка MP4, AVI, MOV, MKV, FLV, WMV, WebM, M4V
- Автоматическое извлечение звука через ffmpeg
- Проверка длительности (максимум 60 минут)
- Нормализация звука перед транскрибацией
- Обработка как в /tmp директории

**Код:**
```python
async def process_video_file(video_bytes: bytes, filename: str, groq_clients: list) -> str:
    # 1. Сохраняем в /tmp
    # 2. Проверяем длительность (ffprobe)
    # 3. Извлекаем звук (ffmpeg)
    # 4. Транскрибируем (Whisper)
    # 5. Чистим /tmp файлы
```

### 2️⃣ **YouTube поддержка** ✅
- **Сначала ищет встроенные субтитры** (русский, английский)
- Если субтитров нет - скачивает видео и извлекает звук
- Использует `youtube-transcript-api` для быстрого получения текста
- Поддерживает youtube.com, youtu.be ссылки

**Алгоритм:**
```
YouTube URL
    ↓
Извлечение video_id
    ↓
Поиск субтитров (ru → en → a.ru → a.en)
    ↓ Есть? → Возвращаем текст
    ↓ Нет
Скачивание видео (yt-dlp)
    ↓
Извлечение звука (ffmpeg)
    ↓
Транскрибация (Whisper)
    ↓
Возвращаем текст
```

**Код:**
```python
async def extract_youtube_subtitles(video_id: str) -> Optional[str]:
    # Использует YouTubeTranscriptApi
    # Пытается найти в порядке: ru → en → a.ru → a.en
    # Возвращает текст субтитров

async def download_video_with_ytdlp(url: str) -> Optional[str]:
    # Использует yt-dlp
    # Скачивает лучший формат (best[ext=mp4]/best)
    # Ограничивает размер (VIDEO_SIZE_LIMIT)
```

### 3️⃣ **TikTok / Rutube / Instagram / Vimeo** ✅
- Единая функция `process_video_url()` для всех платформ
- Автоматическое определение типа платформы
- Скачивание через yt-dlp (универсальный инструмент)
- Извлечение звука и транскрибация

**Поддерживаемые платформы:**
```python
'youtube': ['youtube.com', 'youtu.be']
'tiktok': ['tiktok.com', 'vm.tiktok.com', 'vt.tiktok.com']
'rutube': ['rutube.ru']
'instagram': ['instagram.com', 'instagr.am']
'vimeo': ['vimeo.com']
```

### 4️⃣ **Улучшенная архитектура видеообработки** ✅

**Класс VideoProcessor:**
```python
class VideoProcessor:
    check_video_duration()      # ffprobe
    extract_audio_from_video()  # ffmpeg
    normalize_audio()           # ffmpeg normalization
```

**Класс VideoPlatformProcessor:**
```python
class VideoPlatformProcessor:
    _validate_url()             # Определение платформы
    extract_youtube_subtitles() # YouTube субтитры
    download_video_with_ytdlp() # Скачивание видео
    process_video_url()         # Основная функция
```

### 5️⃣ **Фоновая очистка временных файлов** ✅
- Новая фоновая задача `cleanup_temp_files()`
- Автоматическое удаление /tmp файлов
- Настраивается через `TEMP_FILE_RETENTION`
- Логирование удалённых файлов

**Код:**
```python
async def cleanup_temp_files():
    while True:
        await asyncio.sleep(config.TEMP_FILE_RETENTION)
        # Удаляет video_* и audio_* файлы старше TEMP_FILE_RETENTION
        # Логирует количество удалённых файлов
```

### 6️⃣ **Интеграция видео в основной поток** ✅
- Новый хэндлер для видеофайлов (`F.document` + видео расширения)
- Обработка ссылок на видеоплатформы в текстовом хэндлере
- Единая логика обработки для всех типов видео
- Правильные статус-сообщения для видео

**Хэндлеры:**
```python
@dp.message(F.photo | F.document)  # Включает видеофайлы
async def file_handler():
    # Автоматически определяет видео по расширению
    # Вызывает process_video_file()

@dp.message(F.text)
async def text_handler():
    # Проверяет, не ссылка ли это на видеоплатформу
    # Вызывает process_video_url()
```

### 7️⃣ **Системные требования** ✅
- Требует `ffmpeg` и `ffprobe` на системе
- Новые зависимости: `yt-dlp`, `youtube-transcript-api`
- Инструкции по установке в README

**Установка ffmpeg:**
- Ubuntu: `sudo apt-get install ffmpeg`
- macOS: `brew install ffmpeg`
- Windows: https://ffmpeg.org/download.html

### 8️⃣ **Конфигурация v3** ✅

**Новые параметры в config.py:**
```python
# VIDEO
VIDEO_MAX_DURATION = 3600  # 60 минут максимально
VIDEO_EXTRACT_AUDIO_FORMAT = "wav"
VIDEO_SUPPORTED_FORMATS = ['mp4', 'avi', 'mov', 'mkv', 'flv', 'wmv', 'webm', 'm4v']

# ВИДЕОПЛАТФОРМЫ
YOUTUBE_ENABLED = True
TIKTOK_ENABLED = True
RUTUBE_ENABLED = True
INSTAGRAM_ENABLED = True
VIMEO_ENABLED = True

# YouTube специфичные
YOUTUBE_PREFER_SUBTITLES = True
YOUTUBE_EXTRACT_AUDIO = True
YOUTUBE_SUBTITLES_LANGS = ['ru', 'en', 'a.ru', 'a.en']

# yt-dlp
YTDLP_QUIET = True
YTDLP_NO_WARNINGS = True
YTDLP_SOCKET_TIMEOUT = 30

# Временные файлы
TEMP_DIR = "/tmp"
CLEANUP_TEMP_FILES = True
TEMP_FILE_RETENTION = 300
```

### 9️⃣ **Таймауты увеличены** ✅
```python
GROQ_TIMEOUT = 120.0  # Было 90, теперь 120 для видео
GROQ_RETRY_COUNT = 3  # Было 2, теперь 3
FILE_SIZE_LIMIT = 100 * 1024 * 1024  # Было 10 MB, теперь 100 MB
VIDEO_SIZE_LIMIT = 500 * 1024 * 1024  # Новый параметр: 500 MB
```

### 🔟 **Улучшенное логирование** ✅
```python
LOG_VIDEO_PROCESSING = True  # Новое логирование видео
logger.info(f"Downloading video from: {url}")
logger.info(f"Video downloaded: {video_path}")
logger.info(f"YouTube subtitles extracted ({lang})")
logger.info(f"Audio extracted successfully")
```

---

## 📊 Сравнение версий

| Параметр | v2.0 | v3.0 |
|----------|------|------|
| Видеофайлы | ❌ | ✅ |
| YouTube | ❌ | ✅ (субтитры + звук) |
| TikTok/Rutube/Instagram/Vimeo | ❌ | ✅ |
| Файловый лимит | 10 MB | 100 MB |
| Таймаут Groq | 90 сек | 120 сек |
| Очистка /tmp файлов | ❌ | ✅ |
| ffmpeg требуется | ❌ | ✅ |
| yt-dlp требуется | ❌ | ✅ |
| Зависимостей | 10 | 12 |

---

## 🔄 Миграция с v2.0 на v3.0

### Шаг 1: Обновить файлы
```bash
# Заменить:
# bot.py → bot_v3.py (переименовать в bot.py)
# processors.py → processors_v3.py (переименовать в processors.py)
# config.py → config_v3.py (переименовать в config.py)
```

### Шаг 2: Установить ffmpeg
```bash
# Ubuntu
sudo apt-get install ffmpeg

# macOS
brew install ffmpeg

# Windows
# Скачайте с https://ffmpeg.org/download.html
```

### Шаг 3: Обновить зависимости
```bash
pip install -r requirements_v3.txt
```

### Шаг 4: Готово!
```bash
python bot.py
```

**Совместимость:** .env файл остаётся без изменений!

---

## 🐛 Известные проблемы и решения

### Проблема: "ffmpeg not found"
**Решение:** Установите ffmpeg (см. выше)

### Проблема: "YouTube не находит субтитры"
**Решение:** Бот автоматически извлечет звук и транскрибирует

### Проблема: "Видео слишком длинное"
**Решение:** Измените `VIDEO_MAX_DURATION` в config.py

### Проблема: "Timeout при скачивании больших видео"
**Решение:** Увеличьте `GROQ_TIMEOUT` и `YTDLP_SOCKET_TIMEOUT`

---

## 📈 Примеры использования

### Пример 1: YouTube с субтитрами
```
Input: https://www.youtube.com/watch?v=dQw4w9WgXcQ
Output: ✅ Найдены русские субтитры (245 слов)
        📝 Выбрать вариант обработки...
```

### Пример 2: YouTube без субтитров
```
Input: https://www.youtube.com/watch?v=example
Processing:
  🔎 Ищу субтитры...
  ❌ Субтитры не найдены
  ⬇️ Скачиваю видео...
  🔊 Извлекаю звук...
  🎧 Транскрибирую...
Output: ✅ Распознано 450 слов
```

### Пример 3: TikTok видео
```
Input: https://vm.tiktok.com/example
Processing:
  🔗 Обрабатываю tiktok видео...
  ⬇️ Скачиваю видео...
  🔊 Извлекаю звук...
  🎧 Транскрибирую...
Output: ✅ Распознано 120 слов
```

### Пример 4: Локальное видео
```
Input: video.mp4 (50 MB, 5 минут)
Processing:
  📁 Обрабатываю файл...
  🎬 Обрабатываю видео...
  🔊 Извлекаю звук...
  🎧 Транскрибирую...
Output: ✅ Распознано 320 слов
```

---

## 🎯 Технические детали

### Очередность при обработке YouTube
1. Попытка получить русские субтитры (`youtube-transcript-api`)
2. Если нет → попытка получить английские субтитры
3. Если нет → попытка получить автоматически сгенерированные (a.ru)
4. Если нет → попытка получить автоматически сгенерированные (a.en)
5. Если ничего нет → скачиваем видео и извлекаем звук

### Видеоплатформы (одинаковый процесс)
1. Валидация URL и определение платформы
2. Скачивание видео через yt-dlp
3. Проверка длительности
4. Извлечение звука через ffmpeg
5. Транскрибация через Whisper API
6. Удаление временных файлов

### Файловая система
```
/tmp/
├── video_<pid>_<timestamp>.<ext>  # Исходное видео
├── audio_<pid>_<timestamp>.wav    # Извлеченный звук
└── [удаляются автоматически]
```

---

## 📦 Зависимости (новые в v3.0)

```
yt-dlp==2024.1.1              # Скачивание видео с любых платформ
youtube-transcript-api==0.6.1 # Извлечение субтитров YouTube
ffmpeg                         # Извлечение звука (системный пакет)
ffprobe                        # Получение информации о видео (системный пакет)
```

---

## 🚀 Деплой на серверы

### Render.com
```yaml
Build Command: pip install -r requirements_v3.txt
Start Command: python bot.py
```
ffmpeg будет установлен автоматически на базовом контейнере.

### Heroku
Требует buildpack для ffmpeg:
```bash
heroku buildpacks:add https://github.com/jonathanong/heroku-buildpack-ffmpeg
heroku buildpacks:add heroku/python
```

### Локальный сервер (Ubuntu)
```bash
sudo apt-get update
sudo apt-get install python3-pip ffmpeg
pip install -r requirements_v3.txt
python bot.py
```

---

## 📝 История версий

**v3.0** (2025) - текущая
- ✅ Обработка видеофайлов
- ✅ YouTube (субтитры + звук)
- ✅ TikTok, Rutube, Instagram, Vimeo
- ✅ Очистка /tmp файлов
- ✅ Улучшенное логирование видео

**v2.0** (2024)
- ✅ Разделение на 3 файла
- ✅ Очистка кэша (10 минут)
- ✅ Автоопределение языка
- ✅ pdfplumber вместо PyPDF2

**v1.0** (2024)
- ✅ Базовая функциональность
- ✅ Текст, голос, изображения, файлы

---

## 🎓 Как это работает внутри

### Архитектура видеообработки
```
User Input (URL или файл)
    ↓
Определение типа (платформа или локальный файл)
    ↓
[YouTube?] → Поиск субтитров → Транскрибация → Обработка текста
    ↓ [Другая платформа?] или [Локальное видео?]
Скачивание/Сохранение видео
    ↓
Проверка длительности (ffprobe)
    ↓
Извлечение звука (ffmpeg)
    ↓
Нормализация звука (ffmpeg)
    ↓
Транскрибация (Whisper API → Groq)
    ↓
Проверка смешивания языков
    ↓
Предложение вариантов обработки
    ↓
[Пользователь выбирает режим]
    ↓
Коррекция/Саммари
    ↓
Экспорт (TXT/PDF)
```

---

**Версия**: 3.0  
**Дата**: 2025  
**Статус**: Production Ready ✅  
**Видео поддержка**: Полная ✅
