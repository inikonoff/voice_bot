# 🤖 Грамотей v3: Enterprise Edition

**Грамотей v3** — это мощный мультимодальный Telegram-бот для интеллектуальной обработки контента. Бот умеет транскрибировать аудио и видео, распознавать текст с изображений (OCR), анализировать документы (PDF, DOCX) и предоставлять умные саммари или коррекцию текста с использованием передовых моделей LLM через Groq API.

---

## 🚀 Основные возможности

### 🎙️ Обработка аудио и видео
*   **VAD (Voice Activity Detection):** Автоматическое отсечение тишины и нарезка аудио на чанки для ускорения транскрибации.
*   **Параллельная транскрибация:** Использование пула ключей Groq для одновременной обработки нескольких частей аудио.
*   **YouTube & Social Media:** Загрузка видео и извлечение аудио из YouTube, TikTok, Instagram, Vimeo и др.
*   **Умные субтитры:** Приоритетное получение официальных или автоматических субтитров YouTube без скачивания видео.

### 📄 Работа с документами и изображениями
*   **Vision OCR:** Распознавание текста с фотографий и скриншотов через Groq Vision.
*   **Анализ документов:** Извлечение текста из PDF (до 50 страниц) и DOCX файлов.
*   **Диалоговый режим:** Возможность задавать вопросы по содержимому загруженного документа в режиме чата.

### ✍️ Интеллектуальная коррекция
*   **Три режима обработки:**
    *   📝 **Как есть:** Исправление ошибок с сохранением оригинальной структуры.
    *   ✨ **Красиво:** Стилистическое улучшение текста, придание ему профессионального вида.
    *   📊 **Саммари:** Краткое изложение сути длинных текстов или транскриптов.

---

## 🛠️ Технологический стек

*   **Framework:** [aiogram 3.x](https://docs.aiogram.dev/) (Asynchronous Telegram Bot API)
*   **LLM API:** [Groq Cloud](https://groq.com/) (Llama 3.1/3.3/4.0 models)
*   **Multimedia:** `ffmpeg`, `yt-dlp`, `pydub`
*   **OCR & Docs:** `pdfplumber`, `python-docx`
*   **Deployment:** [Render.com](https://render.com/) (с поддержкой Port Binding)

---

## 📦 Установка и запуск

### 1. Системные зависимости
Для работы с мультимедиа в системе должны быть установлены `ffmpeg` и `ffprobe`:
```bash
sudo apt-get update && sudo apt-get install -y ffmpeg
```

### 2. Установка Python-пакетов
```bash
pip install -r requirements.txt
```

### 3. Настройка окружения (.env)
Создайте файл `.env` и добавьте в него следующие переменные:
```env
BOT_TOKEN="ваш_токен_телеграм_бота"
GROQ_API_KEYS="ключ1,ключ2,ключ3"
PORT=10000 # Для Render
```

### 4. Запуск
```bash
python bott.py
```

---

## 🌐 Развертывание на Render.com

Бот полностью оптимизирован для работы на Render:
1.  **Port Binding:** В `bott.py` интегрирован мини-веб-сервер на `aiohttp`, который отвечает на запросы Render по порту `10000`, предотвращая ошибку "No open ports detected".
2.  **Graceful Shutdown:** Бот корректно завершает работу при получении сигнала `SIGTERM`, очищая временные файлы и закрывая сессии.
3.  **Health Check:** Доступен эндпоинт `/health` для мониторинга состояния.

---

## 🏗️ Архитектура Enterprise Edition

*   **GroqClientManager:** Умная ротация API-ключей, обработка Rate Limits и автоматическое переключение на резервные ключи.
*   **AudioProcessor:** Конвейерная обработка звука (Сжатие -> VAD -> Чанки -> Параллельная транскрибация).
*   **DialogueManager:** Управление контекстом документов для ответов на вопросы пользователей.
*   **Async Subprocesses:** Все тяжелые операции (`ffmpeg`, `yt-dlp`) вынесены в отдельные потоки через `asyncio.to_thread`, что гарантирует отзывчивость бота.

---

## 📝 Лицензия
Проект поставляется "как есть" для частного использования. Все права на используемые API принадлежат их соответствующим владельцам.
