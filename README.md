# Грамотей v3.0 — Telegram Bot для редактирования текста и видео

Мощный бот для распознавания, коррекции и обработки текста с поддержкой:
- 🎙️ Транскрибации голосовых сообщений и кружочков (Whisper v3)
- 📸 Распознавания текста с изображений (Vision OCR)
- 📄 Обработки файлов (PDF, DOCX, TXT)
- 🎥 **Новое v3.0:** Обработки видеофайлов (MP4, AVI, MOV и т.д.)
- 🔗 **Новое v3.0:** Извлечения текста из YouTube (сначала субтитры → звук)
- 📱 **Новое v3.0:** Поддержки TikTok, Rutube, Instagram, Vimeo
- ✨ Трёх режимов редактирования (базовая коррекция, премиум, саммари)

## 🚀 Что нового в v3.0

### Видеоподдержка
- ✅ Обработка локальных видеофайлов (MP4, AVI, MOV, MKV, FLV, WMV, WebM, M4V)
- ✅ Автоматическое извлечение звука из видео
- ✅ Проверка длительности видео (максимум 60 минут)
- ✅ Нормализация звука перед транскрибацией

### Видеоплатформы
- ✅ **YouTube**: сначала ищет субтитры (русский, английский), если их нет - извлекает звук
- ✅ **TikTok**: скачивание и обработка видео
- ✅ **Rutube**: поддержка российского видеохостинга
- ✅ **Instagram**: обработка видео с Instagram
- ✅ **Vimeo**: поддержка Vimeo видео

### Оптимизации
- ✅ Фоновая очистка временных файлов
- ✅ Поддержка параллельной обработки множества видео
- ✅ Улучшенное логирование видео-операций
- ✅ Увеличенный таймаут (120 сек) для обработки видео

## 📋 Требования

### Системные зависимости
Необходимо установить **ffmpeg** и **ffprobe**:

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

**Windows:**
Скачайте с https://ffmpeg.org/download.html или используйте:
```bash
choco install ffmpeg
```

### Python зависимости
```bash
pip install -r requirements_v3.txt
```

**Ключевые пакеты:**
- `aiogram==3.3.0` — Telegram Bot Framework
- `openai==1.3.8` — Groq API client
- `yt-dlp==2024.1.1` — скачивание видео (YouTube, TikTok, и т.д.)
- `youtube-transcript-api==0.6.1` — извлечение субтитров YouTube
- `pdfplumber==0.10.3` — обработка PDF
- `python-docx==0.8.11` — обработка DOCX

## ⚡ Быстрый старт

### 1. Установка

```bash
# Клонируйте/скачайте файлы
git clone <repo> my_bot
cd my_bot

# Установите зависимости
pip install -r requirements_v3.txt

# Убедитесь что ffmpeg установлен
ffmpeg -version
```

### 2. Конфигурация

```bash
# Скопируйте пример .env
cp .env.example .env

# Отредактируйте .env и добавьте:
BOT_TOKEN=your_telegram_bot_token
GROQ_API_KEYS=your_groq_api_key_1,your_groq_api_key_2
PORT=8080
```

### 3. Запуск

```bash
python bot.py
```

Готово! Бот запустится и будет готов к обработке видео. 🎉

## 🎯 Использование

### Текст
Просто отправьте текст сообщением.

### Голос / Кружочки
Отправьте голосовое сообщение или "кружочек" в Telegram.

### Изображения
Отправьте фото с текстом для OCR распознавания.

### Локальные видео
Отправьте видеофайл (MP4, AVI, MOV и т.д.) в чате. Бот:
1. Проверит длительность (максимум 60 минут)
2. Извлечет звук из видео
3. Транскрибирует звук
4. Предложит варианты обработки

### YouTube / TikTok / Rutube / Instagram / Vimeo
Просто отправьте ссылку на видео:
- **YouTube**: https://www.youtube.com/watch?v=... или https://youtu.be/...
- **TikTok**: https://www.tiktok.com/@.../video/... или https://vm.tiktok.com/...
- **Rutube**: https://rutube.ru/video/...
- **Instagram**: https://www.instagram.com/p/... или https://instagr.am/...
- **Vimeo**: https://vimeo.com/...

Бот будет:
1. Для **YouTube**: сначала попытается получить субтитры (русский/английский)
2. Если субтитров нет: скачает видео и извлечет звук
3. Для других платформ: скачает видео и извлечет звук
4. Транскрибирует и предложит варианты обработки

## 🔧 Конфигурация (config.py)

### Видео параметры
```python
VIDEO_MAX_DURATION = 3600          # 60 минут максимально
VIDEO_EXTRACT_AUDIO_FORMAT = "wav" # Формат звука
VIDEO_SUPPORTED_FORMATS = ['mp4', 'avi', 'mov', 'mkv', 'flv', 'wmv', 'webm', 'm4v']

# YouTube
YOUTUBE_PREFER_SUBTITLES = True    # Сначала субтитры
YOUTUBE_EXTRACT_AUDIO = True       # Если субтитров нет - звук
YOUTUBE_SUBTITLES_LANGS = ['ru', 'en', 'a.ru', 'a.en']  # Порядок поиска
```

### Временные файлы
```python
TEMP_DIR = "/tmp"                  # Директория для временных файлов
CLEANUP_TEMP_FILES = True          # Удалять временные файлы
TEMP_FILE_RETENTION = 300          # 5 минут хранения
```

### Таймауты
```python
GROQ_TIMEOUT = 120.0               # 120 сек для видео
VIDEO_MAX_DURATION = 3600          # 60 минут максимум
```

## 📊 Поток обработки видео

```
YouTube ссылка
    ↓
[Поиск субтитров] ← Есть? ✅ → Возвращаем текст
    ↓ Нет
[Скачивание видео]
    ↓
[Проверка длительности] ← > 60 мин? ❌ → Ошибка
    ↓
[Извлечение звука] → ffmpeg
    ↓
[Нормализация звука] → ffmpeg (опционально)
    ↓
[Транскрибация] → Whisper API
    ↓
[Проверка смешивания языков]
    ↓
[Предложение вариантов обработки]
```

## 🎥 Примеры

### Пример 1: YouTube с субтитрами
```
Пользователь: https://www.youtube.com/watch?v=dQw4w9WgXcQ
Бот:
🔗 Обрабатываю youtube видео...
🔎 Ищу субтитры в YouTube видео...
✅ Найдены русские субтитры (245 слов)
[Предлагает варианты обработки]
```

### Пример 2: YouTube без субтитров
```
Пользователь: https://www.youtube.com/watch?v=...
Бот:
🔗 Обрабатываю youtube видео...
🔎 Ищу субтитры в YouTube видео...
⬇️ Скачиваю видео...
🔊 Извлекаю звук из видео...
🎧 Транскрибирую звук...
✅ Распознано 450 слов
[Предлагает варианты обработки]
```

### Пример 3: Локальное видео (MP4)
```
Пользователь: [отправляет video.mp4 (50 МБ, 5 минут)]
Бот:
📁 Обрабатываю файл...
🎬 Обрабатываю видео...
🔊 Извлекаю звук из видео...
🎧 Транскрибирую звук...
✅ Распознано 320 слов
[Предлагает варианты обработки]
```

## 🐛 Отладка

### Проверить ffmpeg
```bash
ffmpeg -version
ffprobe -version
```

### Включить DEBUG логирование
В `config.py`:
```python
LOG_LEVEL = "DEBUG"
LOG_VIDEO_PROCESSING = True
```

### Основные ошибки

| Ошибка | Решение |
|--------|---------|
| `ffmpeg not found` | Установите ffmpeg (см. выше) |
| `Video too long` | Видео более 60 минут (измените `VIDEO_MAX_DURATION`) |
| `No subtitles found` | YouTube видео без субтитров (будет извлечен звук) |
| `yt-dlp not installed` | `pip install yt-dlp` |
| `Timeout` | Увеличьте `GROQ_TIMEOUT` в config.py |

## 📁 Структура файлов

```
bot_v3/
├── bot.py              # Главный файл (запускайте только его!)
├── processors.py       # Обработчики (включая видео)
├── config.py           # Конфигурация (включая параметры видео)
├── requirements_v3.txt # Зависимости
├── .env               # Ваши секреты
├── README.md          # Эта документация
└── /tmp/              # Временные файлы (автоматически удаляются)
```

## 🚀 Деплой на Render.com

1. Создайте новый **Web Service** на https://render.com
2. Подключите GitHub репозиторий
3. В **Settings** установите переменные окружения:
   - `BOT_TOKEN=...`
   - `GROQ_API_KEYS=...`
   - `PORT=8080`
4. В **Build** установите:
   - Build Command: `pip install -r requirements_v3.txt`
   - Start Command: `python bot.py`
5. **Deploy**

Render автоматически установит `ffmpeg` на базовом контейнере.

## 🔐 Безопасность

- **API ключи** в `.env` (не коммитить в Git)
- **Размер файлов** ограничен (100 MB для локальных файлов)
- **Длительность видео** ограничена (60 минут максимум)
- **Таймауты** предотвращают зависание
- **Временные файлы** автоматически удаляются

## 📊 Статистика и мониторинг

Команда `/status` показывает:
```
🤖 Статус бота:
• Groq клиентов: 2
• Пользователей в памяти: 5
• Vision доступен: ✅
• PDF обработка: ✅ (pdfplumber)
• Видео обработка: ✅ (ffmpeg)
• YouTube поддержка: ✅ (yt-dlp)
• DOCX обработка: ✅
• Временная память: 3 файлов
```

## 📚 Документация по платформам

### YouTube
- Сначала ищет встроенные субтитры
- Поддерживает автоматически сгенерированные субтитры
- Если субтитров нет - извлекает и транскрибирует звук
- Максимум: 60 минут видео

### TikTok
- Автоматическое скачивание через yt-dlp
- Извлечение звука из видео
- Полная поддержка всех типов контента

### Rutube
- Российский видеохостинг
- Поддержка всех типов видео
- Автоматическое извлечение звука

### Instagram
- Поддержка видео в постах
- Поддержка Reels
- Автоматическое скачивание и обработка

### Vimeo
- Поддержка публичных видео
- Автоматическое скачивание через yt-dlp
- Извлечение и транскрибация звука

## 🎯 Следующие шаги (возможные улучшения)

1. **Audio normalization** — нормализация громкости перед отправкой
2. **Speaker identification** — определение разных говорящих
3. **Timestamp preservation** — сохранение временных меток при транскрибации
4. **Batch processing** — обработка нескольких видео одновременно
5. **Webhook support** — интеграция с другими сервисами

## 📝 История версий

**v3.0** (текущая)
- Поддержка видеофайлов
- Поддержка YouTube (субтитры + звук)
- Поддержка TikTok, Rutube, Instagram, Vimeo
- Фоновая очистка временных файлов
- Улучшенное логирование видео

**v2.0**
- Разделение на 3 файла (bot.py, processors.py, config.py)
- Очистка кэша (10 минут)
- Автоопределение языка при транскрибации
- Замена PyPDF2 на pdfplumber

**v1.0**
- Базовая функциональность
- Текст, голос, изображения, файлы

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи (`/status` в боте)
2. Убедитесь что `ffmpeg` установлен
3. Проверьте `GROQ_API_KEYS` в `.env`
4. Включите DEBUG логирование в `config.py`

---

**Версия**: 3.0  
**Дата**: 2025  
**Статус**: Production Ready ✅  
**Статус видео**: Полностью поддерживается ✅
