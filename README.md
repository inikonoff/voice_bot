# Грамотей — Telegram Bot для редактирования текста

Мощный бот для распознавания, коррекции и обработки текста с поддержкой:
- 🎙️ Транскрибации голосовых сообщений (Whisper v3)
- 📸 Распознавания текста с изображений (Vision OCR)
- 📄 Обработки файлов (PDF, DOCX, TXT)
- ✨ Трёх режимов редактирования (базовая коррекция, премиум коррекция, саммари)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```
BOT_TOKEN=your_telegram_bot_token_here
GROQ_API_KEYS=your_groq_api_key_1,your_groq_api_key_2
PORT=8080
```

**Где получить ключи:**
- **Telegram Bot Token**: напишите [@BotFather](https://t.me/botfather) → `/newbot`
- **Groq API Keys**: зарегистрируйтесь на [console.groq.com](https://console.groq.com)

### 3. Запуск бота

```bash
python bot.py
```

Бот запустится и начнёт ожидать сообщений.

## 📁 Структура проекта

```
├── bot.py              # Главный файл: хэндлеры, управление контекстом
├── processors.py       # Обработчики: OCR, транскрибация, коррекция
├── config.py           # Конфигурация: промпты, константы, параметры
├── requirements.txt    # Зависимости
├── .env               # Секреты (не коммитить!)
├── .env.example       # Пример .env
└── README.md          # Эта документация
```

## 🎯 Основные возможности

### Распознавание голосовых сообщений
- **Модель**: Whisper Large v3 Turbo
- **Языки**: Автоопределение (поддерживает русский, английский, и 90+ языков)
- **Улучшения**: 
  - Проверка на смешивание языков
  - Детектирование неуверенных распознаваний
  - Логирование определённого языка

### Распознавание текста с изображений
- **Модель**: Meta Llama 4 Scout Vision
- **Поддерживаемые форматы**: JPG, PNG, GIF, BMP, WebP
- **Особенности**:
  - Сохранение структуры текста (абзацы, списки)
  - Сохранение математических формул
  - Сохранение спецсимволов
  - Отметка неясного текста как `[НЕЯСНО]`

### Обработка файлов
- **PDF**: с помощью `pdfplumber` (поддержка таблиц)
- **DOCX**: с помощью `python-docx`
- **TXT**: с автоопределением кодировки (UTF-8, CP1251, KOI8-R и т.д.)

### Три режима редактирования

#### 📝 Как есть (Базовая коррекция)
- Исправление орфографических ошибок
- Базовая пунктуация
- Минимальное вмешательство

#### ✨ Красиво (Премиум коррекция)
- Деликатная коррекция с сохранением стиля
- Сохранение разговорных оборотов
- Замена мата на литературные аналоги

#### 📊 Саммари (Саммаризация)
- Сжатие текста до ~25% объёма
- Адаптивная стратегия по жанру
- Сохранение ключевых деталей

## ⚙️ Конфигурация

Все параметры находятся в `config.py`:

### Таймауты и лимиты
```python
CACHE_TIMEOUT_SECONDS = 600      # 10 минут для удаления контекста
CACHE_CHECK_INTERVAL = 300        # Проверка каждые 5 минут
MAX_CONTEXTS = 1000               # Максимум одновременных контекстов
FILE_SIZE_LIMIT = 10 * 1024 * 1024  # 10 MB
GROQ_TIMEOUT = 90.0               # Таймаут для Groq API
```

### Параметры моделей
```python
VISION_TEMPERATURE = 0.0          # 0 = максимум детерминизма (хорошо для OCR)
TRANSCRIPTION_TEMPERATURE = 0.0   # 0 = точная транскрибация
BASIC_CORRECTION_TEMPERATURE = 0.1
PREMIUM_CORRECTION_TEMPERATURE = 0.2
SUMMARIZATION_TEMPERATURE = 0.3
```

### Пороги обработки
```python
MIN_TEXT_LENGTH = 10              # Минимум символов для обработки
MIN_WORDS_FOR_SUMMARY = 50        # Минимум слов для саммари
MIN_CHARS_FOR_SUMMARY = 300       # Минимум символов для саммари
PREVIEW_LENGTH = 200              # Длина превью
```

## 📊 Управление кэшем

Бот автоматически:
- **Удаляет контексты** старше 10 минут (экономит память)
- **Проверяет кэш** каждые 5 минут
- **Ограничивает количество** одновременных контекстов (максимум 1000)
- **Логирует очистку** для отладки

Это предотвращает утечки памяти при длительной работе.

## 🔧 Команды бота

- `/start` — информация о боте и возможности
- `/help` — подробная инструкция по использованию
- `/status` — статус бота (количество клиентов, контекстов)

## 💬 Взаимодействие с пользователем

### Отправка текста
1. Пользователь отправляет текст/голос/фото/файл
2. Бот показывает превью и предлагает выбрать режим обработки
3. Пользователь выбирает режим
4. Бот обрабатывает и показывает результат

### Переключение режимов
После обработки можно:
- Переключиться на другой режим (результат будет закэширован)
- Экспортировать в TXT или PDF

## 📝 Логирование

Логирование настраивается в `config.py`:

```python
LOG_LEVEL = "INFO"
LOG_TRANSCRIPTION_LANGUAGE = True  # Логировать определённый язык
```

Уровни логирования:
- `DEBUG` — детальная информация для отладки
- `INFO` — основные события (статус, успехи)
- `WARNING` — предупреждения (ошибки, переповторы)
- `ERROR` — серьёзные ошибки

## 🌐 Деплой

### На Render

1. Создайте новый Web Service на [render.com](https://render.com)
2. Подключите GitHub репозиторий
3. Установите Start Command: `python bot.py`
4. Добавьте переменные окружения в Settings:
   - `BOT_TOKEN`
   - `GROQ_API_KEYS`
   - `PORT=8080`
5. Боту нужны постоянные контейнеры (не Cron Job)

### На Heroku (если их поддержка вернётся)

```bash
heroku create your-app-name
heroku config:set BOT_TOKEN=your_token GROQ_API_KEYS=your_keys
git push heroku main
```

### Локально

Просто запустите:
```bash
python bot.py
```

## 🐛 Отладка

### Включите DEBUG логирование

В `config.py`:
```python
LOG_LEVEL = "DEBUG"
LOG_TRANSCRIPTION_LANGUAGE = True
```

### Проверьте статус бота

Отправьте команду `/status` в боте.

### Основные ошибки

| Ошибка | Решение |
|--------|---------|
| `BOT_TOKEN not found` | Проверьте `.env` файл |
| `No Groq clients` | Проверьте `GROQ_API_KEYS` в `.env` |
| `Timeout error` | Увеличьте `GROQ_TIMEOUT` в `config.py` |
| `pdfplumber not installed` | Запустите `pip install pdfplumber` |

## 🔐 Безопасность

- **API ключи**: используйте `.env` файл, не коммитьте в Git
- **Размер файлов**: ограничивает до 10 MB
- **Таймауты**: предотвращают зависание
- **Уровень доступа**: контексты персональные (по user_id)

## 📦 Зависимости

- **aiogram** — Telegram Bot Framework
- **openai** — Groq API client
- **aiohttp** — Web server for health checks
- **pdfplumber** — PDF processing с поддержкой таблиц
- **python-docx** — DOCX processing
- **reportlab** — PDF generation
- **python-dotenv** — Environment variables

## 🤝 Поддержка и контакты

Если у вас есть вопросы или нашли баг:
1. Проверьте логи (`/status`)
2. Прочитайте ошибку в консоли
3. Проверьте FAQ выше

## 📄 Лицензия

Этот проект используется для собственных целей.

---

**Версия**: 2.0  
**Последнее обновление**: 2025  
**Статус**: Production Ready ✅
